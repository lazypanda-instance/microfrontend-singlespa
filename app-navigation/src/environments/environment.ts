export const environment = {
  production: false,
  baseURL: 'http://localhost:4400/'
};

export const endPoint = {
  navigation: {
    MENU: `${environment.baseURL}/assets/stub-json/menu.json`
  }
};

